export default function SignInPage() {
  return (
    <div>
        <h1>Sign In Page</h1>
        <p>Username, Password, we can use OAuth here to have other services like google or facebook to give authentication tokens to log in as well (Maybe)</p>
    </div>
  );
}
